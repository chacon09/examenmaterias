from django.conf.urls import url
from . import views


urlpatterns = [
    #url(r'^$', views.lista_peliculas, name ='lista_peliculas'),
    #url(r'^nota/nueva/$', views.nota_nueva, name='nota_nueva'),
    ]
